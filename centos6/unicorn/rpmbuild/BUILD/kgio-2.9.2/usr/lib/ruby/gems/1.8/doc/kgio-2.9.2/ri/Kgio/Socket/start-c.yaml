--- !ruby/object:RI::MethodDescription 
aliases: []

block_params: 
comment: 
- !ruby/struct:SM::Flow::VERB 
  body: "     addr = Socket.pack_sockaddr_in(80, 'example.com')\n     Kgio::Socket.start(addr) -&gt; socket\n\n     addr = Socket.pack_sockaddr_un(&quot;/path/to/unix/socket&quot;)\n     Kgio::Socket.start(addr) -&gt; socket\n"
- !ruby/struct:SM::Flow::P 
  body: Creates a generic Kgio::Socket object and initiates a non-blocking connection. The caller should select/poll on the socket for writability before attempting to write or optimistically attempt a write and handle :wait_writable or Errno::EAGAIN.
full_name: Kgio::Socket::start
is_singleton: true
name: start
params: |
  

visibility: public
